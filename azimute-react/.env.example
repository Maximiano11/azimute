# API publica usada pelo frontend em deploy.
# Localmente, o Vite usa o proxy de /api para http://localhost:4100.
# Em Vercel/Netlify, configure este valor no painel do provedor.
VITE_API_URL=https://sua-api-publica.example.com/api
